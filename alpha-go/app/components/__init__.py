"""Streamlit UI components for the alpha-go dashboard.

Each component is a thin render function that reads from / writes to the stable service
interfaces (HybridRetriever, financials panel, rag.answer). They render a clear
"not yet implemented" placeholder until their backing phase lands. See docs/ROADMAP.md.
"""
