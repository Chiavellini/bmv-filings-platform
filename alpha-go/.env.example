# Copy to .env (gitignored) and fill in. Only needed for the opt-in LLM sentiment / Q&A layers.
# Sentiment (configs/alpha_go.yaml -> sentiment.provider: deepseek) reads:
DEEPSEEK_API_KEY=

# Q&A (configs/alpha_go.yaml -> qa.provider: anthropic) reads, only if you enable qa:
# ANTHROPIC_API_KEY=
