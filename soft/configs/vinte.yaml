# Vinte — minimal coverage config (XBRL-sourced fundamentals; no custom extractor).
company:
  name: Vinte
  ticker: "VINTE MM"
  currency: MXN
  exchange: BMV
  unit: millions

tier_precedence:
  default: [xbrl, bmv, prose, search, table, calc]

ir_website:
  xbrl_ticker: "VINTE"
