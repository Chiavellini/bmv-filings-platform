"""Corpus layer — turn raw IR sources into a parsed, indexed-ready document corpus.

Reuses the vendored download + parse phases; adds corpus-level manifest/metadata.
See docs/architecture/system_design.md (Ingest phase).
"""
